# Anvil Hub Configuration
# Copy to .env and adjust values

# HTTP port
PORT=4400

# SQLite database directory (created automatically)
DATA_DIR=./data

# Admin bearer token — CHANGE THIS before deploying!
ADMIN_TOKEN=change-me-to-a-random-secret

# Set to "true" to seed example packages on first boot
SEED=true
